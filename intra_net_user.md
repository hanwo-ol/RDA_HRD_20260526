파이썬에 내장된 `help()`와 `dir()` 함수는 인터넷이 없는 환경은 물론, 평소 코딩을 할 때도 매우 강력한 디버깅 및 탐색 도구입니다. 두 함수의 역할과 실무에서의 구체적인 활용법을 나누어 설명해 드립니다.

---

### 1. help() 함수: 함수의 "상세 설명서" 정독하기

`help()`는 특정 모듈, 함수, 클래스, 객체에 작성된 공식 문서(Docstring)를 콘솔 창에 그대로 출력해 주는 함수입니다. "이 함수에 어떤 파라미터를 넣어야 하지?"를 고민할 때 사용합니다.

**주요 출력 내용:**

* **함수 시그니처:** 함수가 기본적으로 어떤 형태를 띠고 있는지 (예: `plot(x, y, [fmt], kwargs)`)
* **요약 설명:** 이 함수가 어떤 역할을 하는지
* **파라미터(Parameters):** 입력할 수 있는 변수들의 이름, 데이터 타입, 기본값, 그리고 각각의 기능 설명 (꾸미기 옵션을 찾을 때 가장 중요한 부분입니다)
* **반환값(Returns):** 함수 실행 후 어떤 결과가 나오는지

**실무 활용 예시:**
산점도를 그릴 때 점의 크기를 바꾸고 싶지만 옵션 이름이 기억나지 않는다고 가정해 보겠습니다.

```python
import seaborn as sns

# 콘솔이나 셀에 아래 코드를 실행합니다.
help(sns.scatterplot)

```

**출력 결과 활용법:**
출력된 텍스트를 쭉 내리다 보면 **Parameters** 섹션이 나옵니다. 여기서 크기와 관련된 단어(size)를 찾으면 다음과 같은 설명을 발견할 수 있습니다.

> **size** : vector or key in `data`
> Grouping variable that will produce points with different sizes.

이를 통해 "아, 점의 크기를 데이터에 따라 다르게 주려면 `size` 파라미터를 쓰면 되는구나"라고 파악하고 코드를 작성하게 됩니다.

---

### 2. dir() 함수: 객체의 "사용 가능한 기능 목록" 훑어보기

`dir()`은 특정 객체나 모듈이 가지고 있는 모든 속성(변수)과 메서드(함수)의 이름을 리스트 형태로 반환합니다. "내가 지금 만든 이 객체로 무엇을 할 수 있지?"를 빠르고 넓게 확인하고 싶을 때 사용합니다.

**특징:**
상세한 설명은 보여주지 않고 오직 '이름'만 나열합니다. 따라서 스펠링이 헷갈리거나, 어떤 기능들이 있는지 대략적인 구조를 파악할 때 유리합니다.

**실무 활용 예시:**
그래프 영역인 `ax` 객체를 만들었는데, 제목이나 축 이름을 설정하는 함수들의 정확한 이름이 기억나지 않는 상황입니다.

```python
import matplotlib.pyplot as plt

fig, ax = plt.subplots()

# ax 객체가 가진 모든 기능 목록을 출력합니다.
print(dir(ax))

```

**출력 결과 활용법:**
수많은 목록(`['__class__', '__delattr__', ..., 'plot', 'scatter', 'set_title', 'set_xlabel', ...]`)이 출력됩니다.

목록이 너무 길어서 보기 힘들 때는 파이썬의 리스트 컴프리헨션을 결합하여 원하는 기능만 필터링해서 찾는 팁이 있습니다. Matplotlib에서 무언가를 설정(세팅)하는 함수는 대부분 `set_`으로 시작한다는 점을 이용하는 것입니다.

```python
# ax의 기능 중 'set_'으로 시작하는 것만 추려보기
set_methods = [method for method in dir(ax) if method.startswith('set_')]
print(set_methods)

```

이 코드를 실행하면 `['set_alpha', 'set_facecolor', 'set_title', 'set_xlabel', 'set_xlim', ...]` 처럼 그래프를 꾸밀 때 사용할 수 있는 함수들의 목록만 깔끔하게 확인할 수 있습니다.

---

### 요약: 언제 무엇을 써야 할까?

* **dir() 먼저:** 어떤 객체가 주어졌을 때, "여기에 쓸 수 있는 함수 이름이 뭐였지?" 하고 전체 목록을 스캔할 때 사용합니다.
* **help() 다음:** `dir()`을 통해 `set_title`이라는 함수를 찾았다면, "이 함수 안에 글꼴 크기나 굵기를 지정하는 파라미터가 뭐지?" 하고 깊게 파고들 때 `help(ax.set_title)`을 사용합니다.
